#include "main.h"
#include <stdio.h>
/**
 * add-adds two numbers
 * Return:the result of the addition
 * @x:argument
 * @y:argument
 */

int add(int x, int y)
{
	int result = x + y;
	return (result);
}
