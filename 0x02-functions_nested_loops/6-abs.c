#include "main.h"
/**
 * _abs-is a function that computes the absolute value of an interger
 * @n:the value to be input as argument
 * Return:the absolute value of an interger
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
		return (-n);
	return (0);
}
